# V1 产品需求文档（PRD）— 单龙虾自动执行引擎

## 1. 背景与问题
- 当前系统已具备聊天、门禁、导出和CI回归能力，但“龙虾”仍以问答为主，无法独立完成代码任务。
- 用户核心诉求：输入一个开发课题，龙虾在可控范围内自动完成“分析-改动-验证-汇报”。
- 业务目标：先做单Agent可执行闭环，再扩展多角色协作群。

## 2. 目标与成功指标
- 产品目标：让单个龙虾具备“读文件、写文件、跑命令、自动重试”能力。
- 成功标准（上线门槛）：
  - 至少完成1个真实任务并自动通过验收命令。
  - 失败场景能自动重试并产出明确失败原因。
  - 全流程可追踪（trace_id、request_id、error_code一致）。
- 核心指标：
  - 任务成功率 ≥ 70%（V1）。
  - 平均重试次数 ≤ 2 次。
  - 危险操作误执行 = 0。

## 3. 用户与角色
- 主用户：项目Owner（你）。
- 系统角色（V1）：
  - `owner`：发起任务、查看结果、取消任务。
  - `coder_agent`：执行任务（有写权限）。
  - `reviewer_agent`：只读+验证（无写权限，可选）。

## 4. 范围定义
- In Scope：
  - 工具层：`list_files/read_file/write_file/run_command`。
  - 执行层：状态机 + 自动重试 + 停机条件。
  - 安全层：路径白名单、命令白名单、敏感文件保护。
  - 验证层：强制执行测试命令与质量门禁。
- Out of Scope：
  - 多Agent群聊协作。
  - 自动发布部署、自动rebase/cherry-pick。

## 5. 功能需求（FR）
- FR-1 任务创建：支持提交 `goal/scope/constraints/acceptance_cmd`。
- FR-2 文件读取：支持按路径+片段读取。
- FR-3 文件写入：支持原子写入（临时文件替换）。
- FR-4 命令执行：仅允许白名单命令，返回 stdout/stderr/exit_code。
- FR-5 自动循环：`PLAN->ACT->VERIFY->RETRY/FINISH`。
- FR-6 失败重试：同类错误最多3次，超限终止并给建议。
- FR-7 结果产物：返回改动文件、测试结果、总结报告。
- FR-8 可取消：任务执行中可取消。

## 6. 非功能需求（NFR）
- 安全：禁止访问项目根目录外路径。
- 稳定：单命令超时可配置（默认120s）。
- 可观测：每个步骤必须记录 trace。
- 一致性：`stop_reason/error_code/request_id` 字段契约固定。

## 7. 安全治理规则
- 路径白名单：`/Users/lixiaoyi/Documents/newtext/agunt`。
- 命令白名单：`python -m unittest`、`pip install -r`、`git status` 等。
- 禁止命令：`rm -rf`、`curl|bash`、系统目录写入。
- 文件保护：`.env*`、私钥、token 文件默认只读。

## 8. 接口设计（V1）
- `POST /api/agent/tasks`
  - 入参：`goal, scope_paths[], forbidden_paths[], acceptance_cmd, max_retry`。
  - 出参：`task_id, status=queued`。
- `GET /api/agent/tasks/{task_id}`
  - 出参：`status, current_step, attempts, logs, stop_reason, error_code`。
- `POST /api/agent/tasks/{task_id}/cancel`
- `GET /api/agent/tasks/{task_id}/artifacts`
  - 出参：`changed_files, test_result, summary`。

## 9. 数据模型（建议）
- `agent_tasks`：`task_id, goal, status, created_at, finished_at, stop_reason, error_code`。
- `agent_task_steps`：`task_id, step_no, step_type, input_json, output_json, ok, latency_ms`。
- `agent_task_artifacts`：`task_id, file_path, change_type, diff_hash`。

## 10. 执行流程
- 1) Owner提交任务。
- 2) Agent产出计划（PLAN）。
- 3) Agent调用工具修改（ACT）。
- 4) 运行验收命令（VERIFY）。
- 5) 失败则重试（RETRY），成功则结束（FINISH）。
- 6) 输出报告：改了什么、为何成功/失败、下一步建议。

## 11. 验收标准（DoD）
- 能自动完成至少1个真实开发任务并通过测试。
- 工具调用日志完整可追踪。
- 命令/路径越权被拒绝且有明确错误码。
- CI工作流通过，不破坏现有质量门禁。

## 12. 里程碑与排期
- M1（1-2天）：工具层 + 安全白名单。
- M2（1-2天）：执行状态机 + 重试策略 + trace。
- M3（1天）：接入质量门禁与CI回归。
- M4（1天）：稳定性修复 + 用户操作指南。

## 13. 风险与预案
- 风险：命令执行越权。
  - 预案：白名单+超时+只读保护。
- 风险：自动修复陷入死循环。
  - 预案：重试上限+错误分类退避。
- 风险：误改核心文件。
  - 预案：scope限制+原子写入+改前快照。

## 14. V2 前瞻（非本期）
- 升级多角色协作群：PM/RD/QA/BA/Finance/SME。
- 增加角色投票与仲裁机制。
- 引入角色级知识库与技能包（skills）。

## 15. 任务输入/输出契约（落地版）
- `POST /api/agent/tasks` 请求体（示例）：
  - `goal`: "为export接口增加字段X并通过测试"
  - `scope_paths`: ["apps/server/src/connectors/web"]
  - `forbidden_paths`: ["apps/server/.env.local", ".github/workflows"]
  - `acceptance_cmd`: "python3 -m unittest -v test_quality_gate_contract.py"
  - `max_retry`: 3
  - `dry_run`: false
- 返回体（示例）：
  - `task_id`, `status`, `created_at_ms`, `request_id`
- `GET /api/agent/tasks/{id}` 返回体：
  - `status`: queued/running/verifying/succeeded/failed/cancelled
  - `step_index`, `attempt`, `stop_reason`, `error_code`, `latest_log`

## 16. 状态机细则（可开发）
- `queued` -> `running`：任务入执行队列。
- `running`：执行 PLAN/ACT；每次工具调用产生日志。
- `verifying`：执行 `acceptance_cmd`。
- `retrying`：验证失败且 `attempt < max_retry`。
- `succeeded`：验证通过。
- `failed`：重试耗尽或命中不可重试错误。
- `cancelled`：用户主动取消。
- 不可重试错误：路径越权、命令越权、写保护文件命中。

## 17. 逐文件实施清单（M1-M3）
- `apps/server/src/core/tool_runtime/tools/file_ops.py`
  - 新增：`list_files/read_file/write_file`。
  - 要求：路径必须在白名单根目录下。
- `apps/server/src/core/tool_runtime/tools/command_ops.py`
  - 新增：`run_command`。
  - 要求：命令白名单、超时控制、stdout/stderr截断。
- `apps/server/src/core/tool_runtime/registry.py`
  - 注册上述工具并配置可见性。
- `apps/server/src/core/orchestrator/agent_runtime.py`
  - 增加执行循环：PLAN->ACT->VERIFY->RETRY。
  - 增加 `max_retry` 与不可重试错误分支。
- `apps/server/src/connectors/web/routes.py`
  - 增加任务API（create/get/cancel/artifacts）。
- `apps/server/src/core/infra/storage/schema.sql`
  - 新增表：`agent_tasks/agent_task_steps/agent_task_artifacts`。
- `apps/server/src/core/infra/storage/sqlite_store.py`
  - 新增上述表的 CRUD 方法。

## 18. 测试用例矩阵
- 工具层：
  - 路径越权读取 -> 拒绝并返回 `PATH_NOT_ALLOWED`。
  - 写保护文件写入 -> 拒绝并返回 `FILE_PROTECTED`。
  - 命令越权 -> 拒绝并返回 `COMMAND_NOT_ALLOWED`。
- 执行层：
  - 验收命令失败 -> 自动重试至上限。
  - 不可重试错误 -> 立即 failed。
  - cancel 在 running 状态生效。
- 集成层：
  - 小任务闭环成功（改1处代码+测试通过）。

## 19. 发布清单（上线前）
- 功能开关：`AGUNT_AGENT_EXEC_ENABLED=1`。
- 白名单配置已生效（路径/命令）。
- CI 包含：工具单测 + 集成用例 + 现有 quality-gate。
- 回滚策略已演练（见下一节）。

### 19.1 当前实现对照（截至本次迭代）
- 功能开关：✅ 已落地 `AGUNT_AGENT_EXEC_ENABLED`（关闭时创建任务返回 `AGENT_EXEC_DISABLED`）。
- 命令白名单：✅ 已收敛到模式白名单（`python -m unittest`、`pip install -r`、`git status`）。
- 受保护文件：✅ 已落地默认保护（`.env*`、私钥/`token` 相关），写入与替换均返回 `FILE_PROTECTED`。
- 安全测试：✅ 已补充契约测试（开关关闭、命令白名单拒绝、受保护文件写入/替换拒绝）。
- 待完成项（高优先级已清空）：
  - 🔜 继续收敛：角色权限矩阵（owner/coder/reviewer）与工具可见性联动。
  - 🔜 发布核对：执行一次完整回归并在发布记录中填入结果。

## 20. 回滚与应急
- 一级回滚（配置级）：关闭 `AGUNT_AGENT_EXEC_ENABLED`。
- 二级回滚（代码级）：回滚到上一稳定tag。
- 事故留痕：保留 `task_id/request_id/trace_id` 便于复盘。

## 21. 开发排期（按天）
- Day 1：文件工具 + 单测。
- Day 2：命令工具 + 安全策略。
- Day 3：执行状态机 + 重试。
- Day 4：任务API + 存储。
- Day 5：联调 + CI + 灰度发布。

## 22. 接口字段字典（研发对表）
- `task_id`：string，必填，任务唯一ID（例：`task_20260101_0001`）。
- `status`：enum，必填，`queued/running/verifying/retrying/succeeded/failed/cancelled`。
- `goal`：string，必填，用户目标描述。
- `scope_paths`：string[]，必填，允许修改目录。
- `forbidden_paths`：string[]，可选，禁止修改路径。
- `acceptance_cmd`：string，必填，验收命令。
- `max_retry`：int，必填，默认3，范围1-5。
- `dry_run`：bool，可选，默认false。
- `stop_reason`：enum，可选，`completed/retry_exhausted/cancelled/policy_denied/runtime_error`。
- `error_code`：string，可选，见错误码字典。
- `request_id`：string，必填，请求级唯一标识。
- `trace_id`：string，必填，链路追踪ID。

## 23. 错误码字典（V1）
- `PATH_NOT_ALLOWED`：访问了白名单外路径。
- `FILE_PROTECTED`：写入受保护文件。
- `COMMAND_NOT_ALLOWED`：执行了未授权命令。
- `COMMAND_TIMEOUT`：命令执行超时。
- `WRITE_ATOMIC_FAILED`：原子写入失败。
- `RETRY_EXHAUSTED`：重试次数耗尽。
- `TASK_CANCELLED`：任务被用户取消。
- `UNEXPECTED_RUNTIME_ERROR`：未分类运行时错误。

## 24. 验收脚本模板（可直接复用）
- 本地验收：
  - `cd /Users/lixiaoyi/Documents/newtext/agunt/apps/server/src`
  - `python3 -m unittest -v test_quality_gate_contract.py`
- CI验收：
  - 触发 `.github/workflows/quality-gate.yml`
  - 要求 `contract-and-gate` 全绿。
- 失败处理：
  - 收集 `step_name + error_summary + last_30_lines`。

## 25. 发布记录模板（每次迭代填写）
- 版本号：`V1.x.y`
- 目标：本次解决的问题。
- 改动文件：列出路径清单。
- 验收结果：本地测试/CI结果。
- 风险评估：高/中/低。
- 回滚方案：配置回滚或代码回滚步骤。

## 26. 原型交互流程（用户视角）
- 场景A：创建任务并自动执行
  - Step 1：用户在“任务输入框”填写 `goal`。
  - Step 2：用户选择 `scope_paths`（可编辑目录）与 `forbidden_paths`。
  - Step 3：用户填写验收命令，点击“开始执行”。
  - Step 4：系统返回 `task_id`，状态显示 `queued -> running`。
  - Step 5：页面实时滚动展示日志（PLAN/ACT/VERIFY）。
  - Step 6：任务结束显示 `succeeded/failed`，并展示改动文件列表。
- 场景B：执行中取消
  - Step 1：用户在任务详情页点击“取消任务”。
  - Step 2：系统状态变更为 `cancelled`。
  - Step 3：页面展示 `stop_reason=cancelled`。
- 场景C：失败重试
  - Step 1：验证失败后系统自动进入 `retrying`。
  - Step 2：页面提示当前重试次数 `attempt/max_retry`。
  - Step 3：超过上限后进入 `failed` 并展示 `error_code`。

## 27. 日志与观测面板定义
- 任务列表字段：
  - `task_id`, `status`, `goal`, `created_at`, `updated_at`, `attempt`。
- 任务详情字段：
  - `request_id`, `trace_id`, `current_step`, `stop_reason`, `error_code`。
- Step日志字段：
  - `step_no`, `step_type(plan/act/verify)`, `tool_name`, `ok`, `latency_ms`。
- 产物区字段：
  - `changed_files[]`, `test_result`, `summary`, `diff_hash`。
- 过滤条件：
  - 按状态过滤（running/failed/succeeded）。
  - 按错误码过滤（如 `COMMAND_NOT_ALLOWED`）。

## 28. 研发联调检查点
- API层：`create/get/cancel/artifacts` 四接口联通。
- 工具层：路径白名单与命令白名单生效。
- 状态机：`queued -> running -> verifying -> retrying/succeeded/failed` 可复现。
- 前端层：任务状态实时更新，无页面假死。
- 观测层：trace/request_id 可检索并与日志对应。

## 29. 权限模型与角色矩阵
- 角色定义（V1）：
  - `owner`：可创建/取消任务，查看全部日志与产物。
  - `coder_agent`：可调用读写文件与命令工具。
  - `reviewer_agent`：只读+验证命令，不可写文件。
- 工具权限矩阵：
  - `list_files`：owner/coder/reviewer 可用。
  - `read_file`：owner/coder/reviewer 可用。
  - `write_file`：仅 coder 可用。
  - `run_command`：coder/reviewer 可用（命令白名单限制）。
- 高风险操作控制：
  - 删除/覆盖关键文件需要二次确认或拒绝执行。
  - 命中黑名单命令直接返回 `COMMAND_NOT_ALLOWED`。

## 30. V1 -> V2 升级路径
- V2目标：从单Agent升级到多Agent协作群（PM/RD/QA/BA/Finance/SME）。
- 升级原则：
  - 先复用 V1 工具层与安全层，不重复造轮子。
  - 新增“角色会话隔离 + 汇总仲裁器”。
- 迁移步骤：
  - Step 1：将 V1 的任务执行器抽象为可复用 `executor_core`。
  - Step 2：新增 `agent_profiles`（角色配置+知识源）。
  - Step 3：新增 `group_orchestrator`（回合制发言+执行分派）。
  - Step 4：新增 `decision_policy`（投票/仲裁/最终结论）。
- 兼容要求：
  - V2 不得破坏 V1 任务接口。
  - V1 的 `quality-gate` 验收链路继续保留并默认启用。

## 31. M1 开发任务拆单（可直接排期）
- T1：文件工具实现（1 人天）
  - 交付：`list_files/read_file/write_file`。
  - 验收：路径越权访问被拒绝。
- T2：命令工具实现（1 人天）
  - 交付：`run_command`（白名单+超时）。
  - 验收：非白名单命令返回 `COMMAND_NOT_ALLOWED`。
- T3：工具注册与权限（0.5 人天）
  - 交付：registry 接入+角色权限矩阵。
  - 验收：reviewer 无法调用写入工具。
- T4：执行状态机（1.5 人天）
  - 交付：PLAN->ACT->VERIFY->RETRY/FINISH。
  - 验收：失败重试到上限后进入 failed。
- T5：任务API（1 人天）
  - 交付：create/get/cancel/artifacts 四接口。
  - 验收：接口契约与字段字典一致。
- T6：存储层（1 人天）
  - 交付：3张任务表 + CRUD。
  - 验收：任务与步骤日志可回放。
- T7：测试与CI（1 人天）
  - 交付：单测+集成用例接入 `quality-gate`。
  - 验收：CI全绿。
- T8：灰度与回滚（0.5 人天）
  - 交付：开关发布与回滚演练记录。
  - 验收：可在5分钟内关闭执行能力。

## 32. UAT 验收清单（产品/测试共用）
- UAT-1：创建任务后状态正确从 `queued` 进入 `running`。
- UAT-2：任务成功时返回改动文件与测试结果。
- UAT-3：任务失败时返回 `error_code/stop_reason`。
- UAT-4：取消任务后状态为 `cancelled`，且不再继续执行。
- UAT-5：路径越权与命令越权均被拒绝。
- UAT-6：日志中可追溯 `task_id/request_id/trace_id`。
- UAT-7：CI 工作流自动触发并通过。

## 33. 交付定义（Release Ready）
- 文档：PRD、接口契约、错误码字典完整。
- 代码：M1 任务拆单全部完成并合并。
- 验证：UAT 7 项全部通过。
- 运维：回滚预案演练完成并留档。

## 34. 启动与日常操作手册（Owner版）
- 本地启动：
  - `cd /Users/lixiaoyi/Documents/newtext/agunt/apps/server/src`
  - `python3 main.py`
  - 浏览器打开 `http://127.0.0.1:8000/`
- 本地回归：
  - `python3 -m unittest -v test_quality_gate_contract.py`
- 提交代码：
  - `git add <files>`
  - `git commit -m "..."`
  - `git push`
- CI检查：
  - 打开 GitHub Actions 查看 `quality-gate` 是否绿色。

## 35. 常见故障排查手册
- 问题：`ModuleNotFoundError: config`
  - 排查：确认仓库包含 `apps/server/src/config.py`。
  - 处理：补提相关文件并重新触发CI。
- 问题：前端 `Invalid or unexpected token`
  - 排查：检查静态JS模板中 `\n` 是否被错误写成真实换行。
  - 处理：将字符串中的换行统一写为 `\\n`。
- 问题：`git push` 网络中断（Broken pipe）
  - 排查：网络稳定性、远程URL是否正确。
  - 处理：重试推送；必要时切 SSH 推送。

## 36. 研发协作规范（单人/多人通用）
- 每次改动必须包含：
  - 目标说明（改什么）。
  - 影响文件清单。
  - 验收命令与结果。
- 禁止直接合并未验证代码。
- 失败日志必须保留关键三项：
  - `step_name`
  - `error_summary`
  - `last_30_lines`
- 每次发布填写第25章发布记录模板。

## 37. 附录：任务模板（复制即用）
- 目标：
  - 例如“为导出接口增加字段X并确保CI通过”。
- 范围：
  - `scope_paths`: `["apps/server/src/connectors/web"]`
- 禁止项：
  - `forbidden_paths`: `["apps/server/.env.local", ".github/workflows"]`
- 验收命令：
  - `python3 -m unittest -v test_quality_gate_contract.py`
- 完成标准：
  - 本地测试通过 + CI `quality-gate` 全绿。
